'use client';

const Header = () => {
  return (
    <header
      className={'h-16 bg-white border-b border-[#E8E8EC] flex justify-end items-center px-6'}
    >
      <span className={'text-sm font-medium text-[#007BFF]'}>Super Admin</span>
    </header>
  );
};

export default Header;
